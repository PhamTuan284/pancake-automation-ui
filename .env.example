# API origin only (no /api, no /health). Trailing slashes are stripped.
# Dev (vite): .env.development — default http://localhost:4001
# Prod (vite build): .env.production — your Railway URL
# Override locally: copy to .env.development.local (gitignored)
VITE_API_BASE_URL=http://localhost:4001
