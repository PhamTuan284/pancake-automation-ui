# Static UI + Caddy (see Dockerfile). Set VITE_API_BASE_URL on the service
# (available at Docker build) to your deployed API origin, no trailing slash.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
